version: "3.8"
services:
  online-shop:
    image: test-jenkins-vite-container
    container_name: test-jenkins-container
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:5173"
